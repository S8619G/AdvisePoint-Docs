# AdvisePoint Docs v1.3.0 final-package verification

September 22, 2026. Final production-mode packages prepared for local installation; no GitHub publication, live upgrade or user-data maintenance was performed.

## Authorization and field evidence

The user reported candidate 13 ARM64 changes working as expected so far and requested final 1.3.0 packages. Separate approval restored normal update controls and selected retirement of both isolated prototype/test data roots through a backup-first utility.

This is not a claim of x64 field acceptance, a complete Windows browser matrix or successful physical printing. The new final launcher configuration and maintenance tools still need Windows testing.

## Changes from candidate 13

- **Application preservation:** All 174 compared files under client/server/shared plus package.json and package-lock.json are byte-identical to the authoritative candidate 13 source. No PDF feature or dependency changes were introduced.
- **Production packaging:** Normal build mode, working-library launcher and update entry points replace candidate-only launch behavior. Version remains 1.3.0.
- **Maintenance:** Backup-first reset ships in production packages and reserves ports 5000, 5100 and 5101. New cleanup targets only the two recognized isolated test roots, backs up and hashes all files before staged removal, and retains recovery copies.
- **Test harness:** An explicit APD_SYNTHETIC_ONLY=1 mode skips unavailable real-manual cases, without weakening the default fixture-required behavior. Application code is unaffected.
- **Documentation:** Consolidated release notes, current installation/testing instructions, START-FRESH.txt, build instructions and regenerated Welcome Guide.

## Fresh automated results

- **Production regression selection:** 303 passed, zero failed, one skipped. Includes 14 maintenance-tool checks (12 cleanup and two reset), updater/update-panel checks and existing library/viewer regressions.
- **Fixture-independent PDF selection:** 30 passed, zero failed, three skipped. Includes protected print-quality/no-print behavior, exact synthetic subsets, cancellation, Chromium/Firefox handoff controls, original attachment, mixed-library backup/restore, restart and reversible removal.
- **Dependency audit:** Zero reported production vulnerabilities.
- **Typecheck:** 14 pre-existing diagnostics remain. This is not a clean TypeScript result.

Unique checks across the two successful final runs: 333 passed, zero failed, four skipped. The separately run 14 maintenance checks are included in that total, not added again.

The core skipped case requires the 724-page rendered fixture to exercise the actual server idle guard. The PDF skips cover supplied-manual integration, supplied-manual range verification, and actual 724-page original attachment. Its restored-page assertion is also intentionally omitted in synthetic-only mode; synthetic retained-original backup restoration still runs.

The dedicated full rendered-print and print-dialog fixture suites were not completed in this fresh session. Earlier candidate evidence remains prior evidence, not new final-build measurements.

## Test setup history

An initial broad PDF run was attempted without the external fixture files. It failed fixture-dependent cases and their downstream assertions, and reported one transient browser `Worker was terminated` error during the integration sequence. Subsequent integration runs passed without any client/server code change.

Test-name filtering did not exclude the fixture cases in this runner configuration. The explicit synthetic-only test mode resolved test selection. The final scoped run passed all 30 executed checks; the omitted real-manual/full-render cases remain unverified in this session.

## Package and source gates

- **ZIP integrity and paths:** Both archives pass integrity and safe-relative-path checks.
- **Architecture:** x64 and ARM64 Node, SQLite and canvas PE machine types match their package labels.
- **Shared application:** Both packages contain the same 213 distribution files, each matching the source build.
- **Runtime preservation:** 1,681 x64 and 1,679 ARM64 runtime/updater files match their respective v1.2.8 baseline bytes, including Node 20.18.1 and native modules.
- **Production identity:** No CANDIDATE, LOCAL_TEST_ONLY, local-test wrapper, candidate readme or alternate application.cjs entry is present. build-mode.json is false for local_test.
- **Standalone tools:** Both maintenance helpers, BAT launchers and START-FRESH.txt match the shared source. Normal startup invokes neither utility.
- **Welcome Guide:** Regenerated with application/document version 1.3.0, extracted-text version checked and cover/body samples visually inspected.
- **Source rebuild:** Independent source extraction and rebuild reproduce all 213 packaged distribution files. See the compact evidence archive for the per-file comparison result.

## Safety tests for the new cleanup utility

Tests exercise exact confirmation, fixed working-library exclusion, both roots copied before removal, empty-directory recovery, missing targets, unknown identity, linked target/recovery folders, occupied ports, active prototype PID, copy failure, backup corruption, source mutation, second-root rename rollback and deletion failure with preserved recovery material.

These tests use disposable Linux folders. Native Windows junction/file-lock behavior and execution of both BAT launchers remain field checks. No destructive utility was run against user data.

## Delivery and remaining limits

Current packages replace the three current build paths, with matching shared source, functional release notes and SHA-256 files. Compact evidence is `verification/v1.3.0/final-evidence.zip`; large fixtures and duplicate release archives are not added to project storage.

GitHub publication and any live installation remain separate actions. No existing library is automatically converted or wiped, and cleanup does not sweep Downloads, browser caches, installed app folders, source manuals or general Windows Temp. The separate Edge idle-connection issue remains unresolved.
