# v1.3.0 candidate 6 verification

September 21, 2026. Local-test packages only. No GitHub publication, update enablement or user-library deletion.

## Change

Removed the shared greater-than-100-page `confirm()` call in PageViewer. Both retained and rendered-page PDF selections now go directly to their existing preparation route. No print backend, worker, resource limit, file format, permission behavior or library storage logic changed. Opening and physical printing remain separate user actions.

Candidate markers, testing instructions, Welcome Guide input and release notes identify candidate 6. The application version remains 1.3.0. This does not address the separate image-PDF size and second-stage browser-loading concern.

## Verification

All 22 tests passed across `pdf-viewer.test.mjs`, `pdf-integration.test.mjs` and `local-candidate.test.mjs`, with the three supplied manuals available. Two added browser tests exercise All pages, a selected range over 100 pages and Current page for each document type. They assert zero native dialogs, correct destination and range, and closure of the viewer's range popover. Retained preparation destinations also return HTTP 200.

The existing integration checks cover retained originals, viewer behavior, restrictions, backup/restore, test-library isolation and disabled update entry points. The npm audit reports zero findings. TypeScript still reports the same 14 pre-existing diagnostics; no new diagnostics were introduced.

Candidate 5's full 724-page generated-PDF validation was not rerun because its backend and worker are unchanged. Its 91.8-second sandbox preparation and approximately 642 MiB output are historical evidence, not candidate-6 laptop timings.

Matching packages are checked for ZIP integrity, native PE architecture, identical shared application files, unchanged v1.2.8 runtime modules/updater, disabled update entry points, candidate markers and updated Welcome Guide. Extracted-source reproduction uses the existing lockfile-matched dependency tree, not a new dependency installation. Verification receipts accompany the saved project files.

## Laptop acceptance

Run candidate 6 from a new extracted folder using its normal Start BAT. It reuses the isolated v1.3.0 test library at port 5101; no reset or reimport is needed. Test All pages on one retained and one rendered-page document on both laptops. The extra browser confirmation must not appear; the preparation controls should behave as before.

Windows preview completeness, physical printing and browser-loading performance remain separate acceptance items.
