# AdvisePoint Docs v1.3.0 candidate 8 verification

September 21, 2026. Local-test x64/ARM64 candidate with shared source; no GitHub publication, update enablement or library reset.

## Change and safety

The rendered-print worker uses a new `print-pixels.cjs` helper. A once-per-worker native-layout probe compares colored/translucent-over-white and odd-width samples against the prior getImageData conversion. Only verified little-endian, aligned RGBA access enables the grouped-packing fast path. Unsupported layouts, missing accessors, mismatched lengths/alignment and native exceptions select the original conversion, permanently for that job after a failure.

The print worker retains white compositing, full resolution, zlib level 3, sequential page processing, file validation, size cap, completion trailer, cancellation and timeouts. No UI or server cleanup changes were needed. The rendered import worker, retained-PDF flows, updater and production data remain unchanged.

## Automated results

| Suite | Passed |
| --- | ---: |
| Core regression, including nine pixel-compatibility tests | 291 |
| Retained-PDF integration and isolated launcher | 14 |
| Rendered-print full manual, corruption, cancellation and limits | 6 |
| Chromium/Firefox dialog and cleanup | 6 |
| Large retained-PDF printing, including supplied manuals | 7 |
| Restricted-import smoke checks | 3 |
| Total without double-counting focused reruns | 327 |

The full 724-page import was intentionally excluded from the three-test import smoke run. The full 724-page rendered-print test was run. Active-import responsiveness remains a Windows field gate; earlier candidate-4 evidence is not presented as a new full rendering run.

Dependency audit: zero reported findings. Typecheck: the same 14 pre-existing diagnostics, not a clean whole-project typecheck.

The first dialog test attempt was blocked by the application's low-temporary-space guard after disposable backup/restore tests consumed sandbox disk. Identified disposable test copies were removed and all six dialog tests passed on rerun. No user files or existing project builds were deleted.

## Complete PDF equivalence and timing

The integrated app prepared all 724 pages in **68,693 milliseconds** and produced **673,101,792 bytes**. SHA-256:

`0af93fd4b218c85ccedea80a52ac827657beb33f92f8e95d8293a3cbfdc55f02`

This matches the verified candidate-7 reference byte-for-byte, whose complete set of 724 decoded page images and dimensions was checked during the investigation. Candidate 8 additionally passed qpdf, page count, exact RGB samples at pages 1, 299, 452 and 724, and HTTP range delivery. Sampled health responses were 1.95–4.22 milliseconds. The temporary QA download was removed after successful validation.

Candidate 7's prior app-level result was 85.3 seconds. The investigation's isolated worker means were 79.35 versus 56.34 seconds across two runs per path, approximately 29% less waiting. These are distinct scopes and runs, not a guaranteed Windows improvement. Candidate 8 still produces the same approximately 642 MiB PDF; later browser loading is not optimized.

## Delivery gates

Matching architecture packages passed safe ZIP path, CRC integrity, candidate/local-test marker, native PE architecture, shared application-file, unchanged v1.2.8 runtime module/updater and versioned welcome-guide checks. Each contains three verified native PE files and the same 212 application files. The new helper is required by source packaging and included in the copied worker directory.

The 255-file shared source archive was independently extracted and rebuilt in local-test mode. All 212 dist files matched the packaged application byte-for-byte, and all nine pixel tests passed again from the extracted source. Rebuild verification reused installed lockfile-matched dependencies; it was not a fresh network install. A subsequent report-only results update does not change application output. Package receipts, source comparison, raw test logs and audit results are retained in compact project evidence.

## Field acceptance

Candidate 7's ARM64 Chrome/Edge/Firefox report was positive. Candidate 8 is new and still needs native Windows x64/ARM64 testing: preparation timing before Open prepared PDF, all 724 pages, reader behavior, cancellation, Done cleanup, Windows PDF associations and physical printing where practical.

Extract into a new folder. The normal candidate launcher uses port 5101 and the existing isolated test library. No reset or reimport is required. GitHub and updates remain disabled pending explicit approval.
